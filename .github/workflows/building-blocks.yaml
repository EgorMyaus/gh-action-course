name: building-blocks

on: workflow_dispatch

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: echo hello
              run: echo hello
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Filed step
              run: |
                echo I will fail
                exit 1
            - name: Say goodbye
              run: echo Goodbye
  